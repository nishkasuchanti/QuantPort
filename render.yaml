services:
  - type: web
    name: quantport-portfolio-optimiser
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /api/health
